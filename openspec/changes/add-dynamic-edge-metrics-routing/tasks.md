## 1. 基础模型、配置与数据库结构

- [x] 1.1 在 `node_metric` 领域包中定义 `EdgeMetric`、协议枚举、聚合指标、协议 cost、边 cost、发布 revision 等核心模型，并为时间、随机数和持久化依赖定义可替换接口。
- [x] 1.2 增加动态指标相关配置，包含功能开关、默认 2 秒采样周期、10 秒聚合窗口、30 秒过期阈值、UDP 丢包惩罚系数 1000、10% 变更阈值、连续 3 次确认和 30 秒最小路由重算间隔，并完成默认值及参数校验测试。
- [x] 1.3 增加 ClickHouse 连接和 `edge_metric_samples` 原始时序表迁移，字段覆盖有向边、协议、延迟、丢包率、速率、观测/接收时间、采样窗口、来源和序列号，并设计适合按时间范围与边查询的分区和排序键。
- [x] 1.4 在 MySQL 中增加 `edge_metric_simulations`、`edge_cost_publications`、`edge_cost_snapshots` 和 `edge_protocol_cost_snapshots` 模型及迁移，建立有向边、协议和 revision 需要的唯一约束与索引。

## 2. 指标上报、存储与查询

- [x] 2.1 实现 `EdgeMetric` 输入校验，校验有向边端点、`tcp`/`udp` 协议、非负延迟与速率、0 到 1 的丢包率、时间戳和样本身份字段，并补充表驱动测试。
- [x] 2.2 实现 ClickHouse 指标仓储：批量写入、基于来源和序列号的幂等处理、按有向边/协议/时间范围查询，以及按配置窗口聚合平均延迟、丢包率、速率。
- [x] 2.3 实现指标接收服务，统一补充服务端 `received_at`、保留客户端 `observed_at`，并将 HTTP 上报和模拟器产生的样本通过同一写入入口持久化。
- [x] 2.4 实现 `POST /api/metrics/report`，返回明确的校验错误和写入结果，并测试 TCP/UDP 上报、重复样本、非法协议及越界指标。
- [x] 2.5 实现 `GET /api/metrics` 的有向边、协议和起止时间过滤，增加分页/数量限制并测试时间段查询结果。
- [x] 2.6 增加 ClickHouse 仓储集成测试，验证原始样本写入、幂等性、时间范围查询和 10 秒窗口聚合结果。

## 3. 可替换的随机指标模拟器

- [x] 3.1 实现 MySQL 模拟配置仓储，支持按 `(from_node_id, to_node_id, proto)` 读取和原子更新启用状态、采样周期及延迟/丢包率/速率随机范围。
- [x] 3.2 实现可注入时钟和随机数生成器的 `MetricSource` 随机实现，按每条有向边与协议独立范围生成指标，并通过固定 seed 测试范围边界和确定性结果。
- [x] 3.3 实现模拟任务调度与生命周期管理，默认每 2 秒生成一次指标，支持配置覆盖、配置变更生效、避免同一任务重入，并在服务退出时优雅停止。
- [x] 3.4 实现 `GET /api/metric-simulations` 和 `PUT /api/metric-simulations`，支持批量读取和保存每条有向边 TCP/UDP 的范围配置，并测试非法上下界和部分更新行为。
- [x] 3.5 验证模拟器只依赖指标接收接口，使后续真实 API/探测器实现可以替换 `MetricSource` 而不改变存储、cost 和路由模块。

## 4. 动态 cost 计算、过期与发布

- [x] 4.1 定义 `ProtocolCostCalculator` 和 `EdgeCostCombiner` 接口，实现 TCP cost 为平均延迟、UDP cost 为平均延迟加平均丢包率乘可配置系数，并统一四舍五入及限制到 1～1000。
- [x] 4.2 实现边级协议合并：TCP/UDP 都有效时取算术平均，只有一个有效时采用该协议 cost；补充单协议、双协议及边界值测试。
- [x] 4.3 实现协议指标过期判定，默认以最新 `observed_at` 超过 30 秒为过期；过期协议不参与合并，全部协议过期时生成 cost 1000 的降级快照但不改变边 status。
- [x] 4.4 实现 cost 防抖候选状态：相对变化达到 10% 后连续 3 次确认才发布，候选方向或数值不满足条件时重置，并覆盖初次发布、零值防护和阈值边界测试。
- [x] 4.5 实现 MySQL cost 发布事务，在一个事务内写入 `edge_cost_publications`、各协议当前快照和边级当前快照，确保 revision 单调且任一写入失败时全部回滚。
- [x] 4.6 实现 `GET /api/edge-costs`，返回边级最终 cost、协议明细、最新指标时间、过期/降级原因和 revision，并测试无快照、单协议过期及双协议有效场景。
- [x] 4.7 实现单实例聚合发布 worker，按配置周期从 ClickHouse 读取窗口数据、计算并发布 cost；重叠 tick 合并或跳过，单次 ClickHouse 失败保留已有快照，直至指标实际过期后再发布 cost 1000。
- [x] 4.8 增加 cost 管线集成测试，覆盖模拟样本经 ClickHouse 聚合、协议公式、合并、防抖和 MySQL revision 原子发布的完整过程。

## 5. 动态 cost 与路由计算联动

- [x] 5.1 扩展 `internal/viewdb` 图加载逻辑：动态模式下以同一已发布 revision 的 `edge_cost_snapshots` 作为权重，`status != 1` 的边始终排除，`status == 1` 且无快照或降级的边以 cost 1000 参与计算。
- [x] 5.2 保留 `graph_edges.cost` 作为静态/人工兼容值且禁止动态流程覆盖；实现动态功能关闭时使用静态 cost 的回退测试。
- [x] 5.3 实现路由重算协调器，在新 cost revision 发布后触发 Floyd 计算，并保证一次计算只读取一个一致 revision。
- [x] 5.4 实现默认 30 秒最小自动重算间隔及触发合并；边 status 变化和全部协议过期进入 cost 1000 时作为紧急事件绕过普通防抖与最小间隔。
- [x] 5.5 实现最新路由结果的原子替换：只有完整计算成功才更新对外结果，计算失败时继续提供上一份完整结果并记录失败状态。
- [x] 5.6 实现 `GET /api/routes/latest`，返回路由结果使用的 cost revision、计算时间和失败/陈旧信息；保留 `/calculate` 作为人工强制重算入口。
- [x] 5.7 增加路由联动测试，覆盖 status 为 0 的边不可用、cost 1000 的边仍可选、同 revision 一致性、最小间隔合并、紧急旁路和失败保留旧结果。

## 6. 测试页面与可观测展示

- [x] 6.1 在测试页面增加有向边/协议模拟配置编辑器，可分别启用 TCP/UDP 并设置延迟、丢包率和速率的最小值、最大值及采样周期。
- [x] 6.2 增加前端输入校验、批量保存和重新加载行为，并通过 JavaScript 测试覆盖非法范围、协议独立配置和保存失败提示。
- [x] 6.3 增加指标历史与当前状态展示，允许按边、协议和时间范围查看原始指标，并显示最新观测时间和过期状态。
- [x] 6.4 在边列表和图形中展示最终动态 cost、TCP/UDP cost、revision 和降级原因，明确区分 `status = 0` 的不可用边与 `status = 1, cost = 1000` 的可用降级边。
- [x] 6.5 展示最新路由计算使用的 cost revision 和计算时间，并保留人工 `/calculate` 操作及错误反馈。

## 7. 应用装配、运维说明与最终验证

- [x] 7.1 在 `cmd/view` 中装配 MySQL、ClickHouse、指标服务、模拟器、cost worker 和路由协调器，确保启动失败清理已创建资源，退出时按顺序停止后台任务并关闭连接。
- [x] 7.2 更新项目 README 和示例配置，说明 MySQL/ClickHouse 初始化、动态功能开关、全部默认参数、API 使用方式以及真实 `MetricSource` 的扩展点。
- [x] 7.3 运行并修复全部 Go 单元测试、数据库集成测试和前端 JavaScript 测试，确保现有静态路由与边状态功能没有回归。
- [x] 7.4 增加端到端测试：在页面配置随机范围，等待 2 秒采样进入 ClickHouse，经聚合和 MySQL cost 发布后自动生成对应 revision 的最新最优路径。
- [x] 7.5 使用 Go race detector 和受控时钟验证并发聚合、重复触发、配置更新及优雅关闭不存在数据竞争、goroutine 泄漏或半发布状态。
